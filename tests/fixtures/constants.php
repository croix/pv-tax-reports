<?php
/**
 * Namespaced constants normally defined by the main plugin file, which cannot
 * be loaded in unit tests because it also boots the plugin.
 *
 * @package PoorVida\TaxReports
 */

declare( strict_types=1 );

namespace PoorVida\TaxReports;

const VERSION    = '0.5.3';
const DB_VERSION = 2;
